apiVersion: apps/v1
kind: Deployment
metadata:
  name: polling-app
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: polling-app
        env:
        - name: ENVIRONMENT
          value: "prod"
